package DTO;

import Model.Korisnik;
import Model.Pacijent;


public class PacijentDTO extends KorisnikDTO{
     private int penali;
     private String pogodnosti;
	 private String alergeni;
     
     public PacijentDTO(Pacijent pacijent) {
 //   	super(pacijent.getIme(), pacijent.getPrezime(),pacijent.getDatum_rodjenja(), pacijent.getEmail(),pacijent.getJmbg(),pacijent.getAlergeni(), pacijent.getGrad(),pacijent.getPenali(),pacijent.getPogodnosti(), pacijent.getTelefon());
       //	for(Object au : pacijent.getAuthorities()) {
    	//		this.setRole(((Authority) au).getName());}
    	
    }



	
	
}
